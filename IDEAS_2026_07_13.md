# Ideation fleet synthesis — 2026-07-13

Ten background agents, one lens each (optimization / architecture / training+data
/ test-time compute / knowledge capacity / learning dynamics / systems / agentic
training / contrarian wildcards / small-model recipe archaeology), each grounded
in this repo's evidence bank and required to return ranked ideas with kill-tests
+ honest novelty checks (papers through 2026-07). Full per-lens reports are in
the session transcripts; this doc keeps the synthesis + every idea's one-liner.

## Cross-lens convergences (independent lenses proposing the same thing)

1. **Selective token loss (Rho-1-style triage).** Training-data lens #1 AND
   optimization lens #2. Score every token with a reference model — either our
   ALREADY-STORED Qwen-7B KD logits (free) or SmolLM2-360M (1 day to score,
   cacheable) — and train only on learnable-not-yet-learned tokens; route
   teacher-KD onto informative tokens, drop noise. Rho-1: SOTA math at 3% of
   tokens. The single most direct attack on token poverty. Kill-test ~1 day.
2. **Decay-phase data upgrade (annealing).** Recipe lens' #1 ("the most
   replicated lever in every small-model report": SmolLM2 MATH 7→22 in decay;
   OpenCoder ablation-validated; OLMo-2 Dolmino; Qwen3 stage-2) + dynamics
   lens' knowledge-late placement + our own WSD structure making decay a
   cheap re-runnable appendix. We currently fill the decay with the SAME mix
   as the plateau — no strong recipe does this. Data downloadable
   (opc-annealing-corpus, Stack-Edu, FineMath4+). + OLMo-2 decay-soup (2-3
   decays, different order, soup) rides on our validated SWA result.
3. **Repair as the central untrained capability.** Test-time lens #1
   (repair-native decoding: sample→grade→feed error_text back→repair; the
   envelope-GROWER selection can't be) + agentic lens #1 (on-policy hindsight
   repair-traces as a ~15% pretrain STREAM, deployment loop then
   distribution-matched) + agentic #5 (repair-step DPO pairs free from the
   grader). All infra exists (gen_rejection_data --keep_all, error_text).
4. **SAM-on-WSD-decay.** Optimization #3 and dynamics #2 independently cite
   the same 2026 result (31% less SFT forgetting, 40% less quantization
   damage, OLMo-1B scale) — targets our chronic dirty-SFT regression + the
   quantized-edge-deploy story. 2x cost on only ~10-15% of steps. Stacks with
   SWA/soup.
5. **Recover the second GPU.** Systems #1: the DDP-latent incompatibility is
   a DistributedDataParallel-HOOK property, not a two-GPU property. Manual
   bucketed allreduce (no hooks, no graph assumptions; grad-accum amortizes
   comm to ~1-2%/step even unoverlapped) or DiLoCo local-SGD fallback.
   ~1.9x effective throughput = the largest single lever under token poverty.
   1-day decisive validation (bandwidth microbench + 500 real latent steps).
6. **Bounded-softmax hybrid, field-confirmed.** Architecture #1 (1-2
   sliding-window softmax layers, W=1024, placed by Gather-and-Aggregate head
   attribution — total state stays ~16MiB) + recipe lens part-3 warning:
   EVERY shipped efficient sub-1B line is hybrid (LFM2 ablated pure-linear
   OUT; Falcon-H1 "small attention fraction sufficient"). The field already
   answered our hybrid-tax fork: "hybrid, minimally." Harness exists.
7. **The latent executor as infrastructure, not headline.** Test-time #2
   (search-native decoding: state-checkpointed program tree search, 8.2MiB
   per branch vs GBs of KV — transformers' active pain point per ArborKV;
   partial-execution pruning + the neural interpreter as value function on
   non-executable prefixes = unpublished) + agentic #3 (simulate-or-shell-out
   router trained on free agreement labels latent-vs-real-execution =
   unpublished) + wildcard #4 (latent microcode: commit-op every ~4 hops
   writes result to state/WM, compose shallow units — attacks the 6-hop
   wall via state management) + wildcard #5 (co-execution DAgger; its FREE
   DIAGNOSTIC: measure hop-horizon under self-generated vs teacher-forced
   intermediates — the ~6-hop horizon may be exposure bias).
8. **The state as a first-class artifact.** Wildcard #1 (State Algebra:
   merge states from disjoint shards → parallel O(1) prefill, zero-training
   repo cartridges, crossover-as-search; State Soup validated the primitive
   on Mamba, delta-rule/repo/search versions open; <1-day probe on existing
   multibind harness; FAILURE IS EQUALLY LOAD-BEARING — order-entangled
   states would constrain the whole meta-TTT design) + knowledge #2 (state
   cartridges via self-study, the Cartridges recipe on an 8.2MiB flat state)
   + wildcard #6 (population-of-states + router).
9. **KD-native everything.** Recipe lens: Gemma-3 (KD for the whole line),
   LFM2 (KD for all 10T; decoupled top-K objective fixing truncated-KD
   support mismatch — drop-in for our bridge), Qwen3 (small models get
   strong-to-weak distillation NOT RL), MobileLLM-R1 ablation (SFT 74.0 vs
   RL 57.0). Converges with our own pass@k finding (RL = greedy sharpening
   only). Also: teacher COMPATIBILITY > teacher strength (a 1.5B coder
   teacher may beat 7B and generates logits 4x faster).

## Per-lens single best bets (verbatim from the agents)

| lens | best bet | cost |
|---|---|---|
| optimization | ship per-head-NS preconditioner (+ activation right-precond) — in-house +7-13% iso-step | 1d A/B |
| architecture | G&A-placed bounded-SWA hybrid (recovers the linear-attn tax within the moat) | 1.5d |
| training/data | teacher-scored token triage (Rho-1 on stored KD logits) | 1d |
| test-time | search-native decoding (state-checkpointed tree search + interpreter value fn) | 2d |
| knowledge | host-RAM giant PKM 262k→8M slots (log-linear knowledge scaling, moat intact) | 2d |
| dynamics | Skill Tower + probe-decodability predictor (which skills need externalization) | 1d |
| systems | two-GPU manual-allreduce/DiLoCo (~1.9x tokens) | 1d validate |
| agentic | on-policy hindsight repair-trace midtraining, gated by EdgeBench-mini | 2d |
| wildcards | State Algebra (merge delta-rule states; informative in both directions) | <1d |
| recipes | KD-through-anneal on the linearized base (decay upsample + decoupled-top-K + FIM + instruct-in-decay + soup) | ~1 GPU-week |

## Recipe-lens field facts worth pinning

- Nobody trains a competitive sub-1B on <2T tokens; we're at 5B → inheritance
  (linearization/KD) is the only coherent move — independently re-validated.
- RADLADS: softmax→linear conversion at 350-700M tokens retains ~90-100%;
  their ablations: hidden-state alignment step load-bearing; NEVER freeze
  MLPs; DCLM > FineWeb-Edu for Qwen teachers; reasoning teachers convert
  worse (measure inference-time-scaling retention after conversion — directly
  relevant to latent-thinking-on-linearized plans).
- Qwen2.5-Coder ablation: 70:20:10 code:text:math BEAT 85:10:5 and 100:0:0.
- FIM: universal (50% of code examples), ~free inside the decay mix, mostly
  benchmark-neutral but the editing capability is the agent north star.
- Deep-thin dominates sub-1B transformers — EXCEPT LFM2 (16L at 350M), the
  one line latency-optimized like us. Linearizing Qwen2.5-Coder-0.5B (24L)
  sidesteps the decision.

## Synthesis: recommended sequencing

**Tier 0 — near-free, this week, no conflicts with running programs:**
1. State Algebra probe (<1 day, CPU/GPU-light, existing harness; informs
   meta-TTT design either way).
2. Exposure-bias diagnostic on the depth-fix ckpt (hours; if the 6-hop
   horizon is teacher-forcing artifact, the fix is training-schedule, not
   architecture).
3. Two-GPU allreduce validation (1 day; if ~1.9x holds, every subsequent run
   in this doc halves in wall-clock). **DONE 2026-07-16: MANUAL-ALLREDUCE
   verdict.** NCCL flat all_reduce of the full 0.80 GB grad (402M bf16) =
   93.8 ms (8.6 GB/s algo-bw); 11×64 MB buckets = 108.7 ms. Unoverlapped
   comm = 1.9% of a 5 s step — far under the pre-registered 15% bar, so
   DiLoCo is unnecessary; a simple hand-rolled bucketed allreduce (no DDP
   hooks → no static_graph, latent-compatible) recovers the second GPU at
   ~1.9× effective throughput. `experiments/bench_two_gpu_allreduce.py`.
4. Execution-gated cascade measurement (~0 GPU; produces the first
   $-per-solved-problem number for the cost-moat thesis).

**Tier 1 — the next production-run package (compose into ONE campaign):**
KD-through-anneal on the linearized-Qwen base (recipe lens' best bet),
with Rho-1 token triage active, per-head-NS shipped, FP8 if the microbench
holds, SAM+soup on the decay, FIM in the decay mix. Each element has an
independent 1-day kill-test; run those first, compose survivors.

**Tier 2 — research bets (sequence behind meta-TTT P1/P2, which is underway):**
search-native decoding (the moat demo), repair-trace midtraining + EdgeBench-
mini, Skill Tower probe, bounded-SWA hybrid arm, host-RAM PKM scaling curve,
simulate-or-shell-out router, latent microcode.

**Explicitly NOT adopted** (agents' own negative findings): sequence-length
warmup (DeltaNet is ~flat in T — the O(T²) mechanism doesn't exist here);
influence-score mixture curation (6,800 GPU-h); trillion-token synthetic
generation; multi-stage stable-phase curricula at our budget (SmolLM2's own
sub-1B finding: single-stage quality-first); amortized-sleep hypernetwork
(meta-scale we don't have — keep the linear state→LoRA probe as a curiosity);
grok-injection (unfavorable prior, attach-pathology risk).

## Tier-1 kill-tests log

### KT-1 (2026-07-14, PRE-REGISTERED): OLMo-2-style microanneal A/B — decay-phase data upgrade (convergence #2)

**Setup.** Two arms from the SAME base ckpt `checkpoints/stageA_executor.pt`
(32L x 960d lean DeltaNet, SmolLM2 tokenizer), identical recipe
(`launch_microanneal_AB.sh`: 2300 steps x 131,072 tok/step ~= 300M tokens,
single GPU, muon lr 6e-4/2e-3, wsd `--warmup_steps 50 --lr_decay_frac 0.85`
= mostly-decay anneal shape, seed 0, 50M-token periodic ckpts via
`--mid_eval_every_tokens 50000000 --mid_eval_save_only`). The ONLY inter-arm
delta is `--data_mix`:
- **control**: `configs/pretrain_mix_feature_pilot.yaml` (the status-quo mix).
- **anneal**: `configs/anneal_mix_v1.yaml` = 40% opc-annealing-corpus
  (0.27 algorithmic / 0.07 synthetic-snippet / 0.06 synthetic-QA — NB the
  upstream HF config names for algorithmic vs QA are SWAPPED vs their
  contents; the yaml maps by verified CONTENT) + 10% finemath-4plus + the
  pilot mix at exactly 0.5x per source.

Launch: `ARM=control ./launch_microanneal_AB.sh` and
`ARM=anneal ./launch_microanneal_AB.sh` (GPU=<id> to pick the card).

**Metrics (both FINAL ckpts, exact commands):**

```
PYTHONPATH=. CUDA_VISIBLE_DEVICES=1 .venv/bin/python experiments/humaneval_solution_ce.py \
  --ckpt checkpoints/microanneal_control.pt --json runs/he_ce_microanneal_control.json
PYTHONPATH=. CUDA_VISIBLE_DEVICES=1 .venv/bin/python experiments/humaneval_solution_ce.py \
  --ckpt checkpoints/microanneal_anneal.pt --json runs/he_ce_microanneal_anneal.json

PYTHONPATH=. CUDA_VISIBLE_DEVICES=1 .venv/bin/python experiments/probe_depdist_stratified_ce.py \
  --ckpt checkpoints/microanneal_control.pt --label microanneal_control \
  --n_seqs 700 --T 3072 --skip 50000 --configs base \
  --data_cache /tmp/scratch/depdist_data_T3072.pt \
  --out runs/depdist_microanneal_control.json
PYTHONPATH=. CUDA_VISIBLE_DEVICES=1 .venv/bin/python experiments/probe_depdist_stratified_ce.py \
  --ckpt checkpoints/microanneal_anneal.pt --label microanneal_anneal \
  --n_seqs 700 --T 3072 --skip 50000 --configs base \
  --data_cache /tmp/scratch/depdist_data_T3072.pt \
  --out runs/depdist_microanneal_anneal.json
```

Both arms share the SmolLM2 tokenizer, so the per-token `ce` from
`humaneval_solution_ce.py` is directly comparable; the shared
`--data_cache` makes the depdist probe score the identical token set for
both arms (compare `table.base.total_ce` and the per-stratum rows).

**Decision rule (pre-registered, before either arm has run):** ADOPT the
anneal mix as the standard decay-phase mix iff
1. HE-solution CE improves by >= 0.01 nats/token:
   `ce(anneal) <= ce(control) - 0.01`, AND
2. the dep-distance-stratified total does not regress:
   `total_ce(anneal) <= total_ce(control) + 0.01` (base config), with no
   single stratum regressing by > 0.05.
Otherwise KILL (keep the pilot mix for decay) and log the deltas here.
Secondary read-only diagnostics (no veto power, noted for the production
decay design): per-stratum long-range rows (256+), the 50M-token ckpt
trajectory.

### KT-1 RESULT (2026-07-14): NOT adopted as-is; KT-1b registered
- HE-solution CE: control 0.7247, anneal **0.6653** (−0.059, 6x the adopt
  threshold — and far below the prior project best 0.7343).
- Dep-distance stratified guard: anneal 1.0894 vs control 1.0543 = **+0.035
  natural-code regression, 3.5x over the ≤+0.01 guard → VETO** (classic
  curated-shift: better at exercise-style code, worse at wild code).
- **KT-1b (registered before launch): anneal_mix_v2 = half-strength anneal**
  (opc+finemath x0.5 ≈ 20%+5%, pilot x1.5 ≈ 75%), SAME control arm reused,
  SAME rule (HE-CE ≥0.01 better AND depdist regress ≤0.01). If v2 also
  trips the guard, try guard-first design (anneal only inside the last-30%
  decay) before abandoning.

### KT-1b RESULT (2026-07-15): photo finish at the guard
- v2 (half-strength anneal): HE-CE **0.6797** (control 0.7247 → keeps −0.045
  of v1's −0.059 win, 76%); depdist total 1.0644 vs control 1.0543 =
  **+0.0101 — misses the ≤+0.0100 guard by 0.0001** (formally a miss; the
  measured point is indistinguishable from the line).
- The trade curve is now mapped and ~linear in anneal strength:
  1.0x → (−0.059 HE, +0.035 dep); 0.5x → (−0.045, +0.010).
- **Disposition: adopt the DESIGN (curated anneal in the decay phase at
  ≤0.5x strength) for the production run's decay, with final strength
  calibrated inside that run against the same guard (a ~0.4x point projects
  to ≈−0.04 HE-CE safely inside). No further 4h micro-arms — the production
  decay re-tunes this anyway.**
